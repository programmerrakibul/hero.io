import { RotateLoader } from "react-spinners";

const PageLoader = () => {
  return (
    <div className="min-h-dvh flex justify-center items-center">
      <RotateLoader color="#7340E7" />
    </div>
  );
};

export default PageLoader;
